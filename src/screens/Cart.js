import React from 'react';
import {View, StyleSheet} from 'react-native';
import Loader from './Loader';

const Cart = () => {
  return (
    <View>
      <Loader />
    </View>
  );
};

export default Cart;
